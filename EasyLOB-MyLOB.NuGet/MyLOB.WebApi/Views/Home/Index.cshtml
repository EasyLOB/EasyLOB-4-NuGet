@{
    string home = ResourcesHelper.GetMenuResource("Home");
    string documentTitle, pageTitle;
    AppHelper.Title(out documentTitle, out pageTitle,
        "", "", home);
    ViewBag.Title = documentTitle;
}
